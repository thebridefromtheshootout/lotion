✅ support column cut paste in table
✅ introduce /copy command only for  when cursor in code snippets and blocks
✅ Disable smart paste in code snippets
✅ /resource command to open a file picker to add to .rsrc other than images, add link to file where cursor was.
✅ /clean-list cmd to remove empty lines in a list.
✅ common ligatures: -> to unicode arrow or emoji configurable setting. Don't add useless ligatures like two alphabets to one. Add what ligatures notion offers.
✅ Date regex must be surrounded by whitespace for codelens to appear
✅ /commit is broken, where does it run git add -A, commit? It was failing the last time I tried it.
✅ /rename-page command (make one if it does not exist) should
    Update backlinks
    Rename folder
    Update index.md's H1
✅ /move-page comand missing. It should offer moving the current active page.
✅ Disable sanitizing of output in processors. Currently, < and > are sanitized out for html portability
✅ update /image command or smart paste to use html img instead of markdown image links.
✅ Move all regex expressions to a single regex const object like Cmd
✅ Update the database code to list db entries in the index.md file for a database with bullet points -. Right now it just lists the entries
Complex Tasks below
[Complex] Emojis break regex rules lmao
[complex]Todo bubble up from nested pages to parent page.
[Complex] Fix paste time
[Complex] Selection links
[Complex ]List renumbering problem in the presence of callouts unindented
[Complex ] -. 1. -> 1. 
[Complex] Image width contraints by converting md link to html with width values
[Complex] Make all edit instruction html|markdown aware.
